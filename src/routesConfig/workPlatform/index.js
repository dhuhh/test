// import mainPageRoutes from './mainPage';

// const prefix = '';

// const addPrefix

// const routes = {
//   mainPageRoutes,
// };

// Object.keys(routes).forEach((key) => {
//   const routesArr = routes[key];
//   routesArr.forEach((item, index) => {
//     if (item.path) {
//       routesArr[index].path = `${prefix}${item.path}`;
//     }
//   });
// });

// export default routes.map((route) => {
//   return {
//     ...route,
//     path: `${prefix}${route.path}`,
//   };
// });
